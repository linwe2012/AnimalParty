using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FishCurve : MonoBehaviour
{
    public AnimationCurve powerCurve;
    public bool flag;
    public float timer;
    //private void


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void LateUpdate()
    {
        //if (flag)
        //{
        //    transform.position -= new Vector3(0, 0.01f, 0) * fishCurve1.Evaluate(timer / 10.0f);
        //    timer += Time.deltaTime;
        //    if (timer > 10.0f)
        //    {
        //        flag = false;
        //        Debug.Log("finish");
        //    }
        //}
    }


}
